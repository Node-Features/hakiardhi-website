# API Configuration
# Leave empty to use Next.js proxy (recommended - avoids CORS issues)
# The proxy is configured in next.config.ts to point to: https://hakiardhi-api.vercel.app
# Or set to backend URL for direct connection (may have CORS issues)
NEXT_PUBLIC_API_URL=
